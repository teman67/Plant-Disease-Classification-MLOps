[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short -m "integration or not integration"
filterwarnings =
    ignore::DeprecationWarning:httpx
markers =
    integration: slow tests that load the real model and read real artifacts (run with: pytest -m integration)
